/**
 * Theme Configuration Re-export
 * Convenience re-export of design tokens from core/theme
 */

export {
  colors,
  typography,
  spacing,
  borderRadius,
  shadows,
  zIndex,
  breakpoints,
  theme,
} from '@/core/theme';

export type {
  Colors,
  Typography,
  Spacing,
  BorderRadius,
  Shadows,
  ZIndex,
  Breakpoints,
} from '@/core/theme';
